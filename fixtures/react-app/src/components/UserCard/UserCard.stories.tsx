// ❌ DECOY for the contested-name locator case (PLAN §5).
// Declares `const UserCard` in a noise path (.stories.) → −35.
const UserCard = (): null => null;
export default { title: 'UserCard', component: UserCard };
